apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: reversewords-dev-app-subscription
  namespace: reversewords-app
spec:
  channel: reversewords-app/mvazquez-gitops-github
  placement:
    placementRef:
      kind: PlacementRule
      name: development-clusters
    packageFilter:
      filterRef:
        name: reversewordsdev-filter
#timeWindow:
#  type: "active"
#  location: "Europe/Madrid"
#  weekdays:                         # Optional, but required for configuring a time window. A list of days, defined as an array, indicates the days of the week when the time range is applied. For example: ["Monday", "Wednesday", "Friday"]
#  hours:
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: reversewordsdev-filter
  namespace: reversewords-app
data:
  branch: stage
