apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: production-clusters
  namespace: gitops-apps
spec:
  clusterLabels:
    matchLabels:
      env: "pro"
  clusterReplicas: 1
